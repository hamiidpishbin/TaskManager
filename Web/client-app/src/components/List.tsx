import React from "react";
import { default as MuiList } from "@mui/material/List";
import ListItem from "./ListItem";


export default function List() {
  return (
    <MuiList>
      {/* {sprints.map((sprint) => (
        <ListItem sprint={sprint} />
      ))} */}
    </MuiList>
  );
}
